- @posts.each do |post|
	%h2= link_to post.title, post
	= link_to (image_tag post.image.url(:small)),post
	%p
		= link_to post.comments.count
		Comments
	%p
		= post.get_likes.size
		Likes
= link_to "Add New Insperation", new_post_path	